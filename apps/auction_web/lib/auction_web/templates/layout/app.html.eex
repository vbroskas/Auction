<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8" />
	<meta http-equiv="X-UA-Compatible" content="IE=edge" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>AuctionWeb · Phoenix Framework</title>
	<link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>" />
	<script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</head>

<body>
	<header>
		<div class="container" style="border-bottom: 1px solid #777777; margin-bottom: 2rem; padding-bottom: 1rem; display: flex; flex-direction: row;">
		<%= if @current_user do %>

		<div style="flex: 1;">
		<%= link @current_user.username, to: Routes.user_path(@conn, :show, @current_user) %></div>

		<div style="flex: 1;"><%= link "Create Auction", to: Routes.item_path(@conn, :new) %></div>
		<div style="flex: 1;"><%= link "Open Auctions", to: Routes.item_path(@conn, :index) %></div>
		<div style="flex: 1;"><%= link "Closed Auctions", to: Routes.closed_path(@conn, :closed) %></div>
		<div style="flex: 1;"><%= link "Log out", to: Routes.session_path(@conn, :delete), method: :delete %></div>

		<% else %>
			<div style="flex: 1;"><%= link "login", to: Routes.session_path(@conn, :new) %></div>
			<div style="flex: 1;"><%= link "register", to: Routes.user_path(@conn, :new) %></div>
		<% end %>
	</div>
	</header>


	<main role="main" class="container">
		<p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
		<p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
		<%= @inner_content %>
	</main>
</body>

</html>
